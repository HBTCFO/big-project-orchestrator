#!/usr/bin/env bash
set -euo pipefail
# BIG_PROJECT_ORCHESTRATOR_PLACEHOLDER=1

# Customize this file with the exact setup steps for new worktrees.
# Example:
# pnpm install --frozen-lockfile
# pnpm build

echo "No custom setup commands configured yet."
echo "Edit .codex/orchestrator/setup.commands.sh to define them."
exit 0
